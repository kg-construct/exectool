DROP TABLE IF EXISTS student1;
DROP TABLE IF EXISTS student2;

CREATE TABLE student1 (KEY SERIAL, name VARCHAR, age VARCHAR, PRIMARY KEY(KEY));
CREATE TABLE student2 (KEY SERIAL, name VARCHAR, age VARCHAR, PRIMARY KEY(KEY));

COMMIT;
